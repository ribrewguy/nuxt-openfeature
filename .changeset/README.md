# Changesets

This directory tracks release notes and semver bumps.

## Create a changeset

```bash
pnpm changeset
```

## Apply versioning changes locally

```bash
pnpm version-packages
```

## Publish (CI)

Publishing is handled by GitHub Actions on version tags.
