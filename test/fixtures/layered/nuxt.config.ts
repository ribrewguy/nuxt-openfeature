// Final app — extends the base layer. Defines NO openFeature config.
// The module should pick up the parent layer's config via _layers merge.
export default defineNuxtConfig({
  extends: ['./base-layer']
})
